# AGENTS.md — 巨亨ONLINE APP Prototype

本檔為本專案的共同工作指引，適用於功能修正、新增功能、介面調整、分析與規格撰寫。保存穩定的工作原則；個別功能規則、待辦與進度放在對應文件中。

## 專案定位

- 本專案是巨亨ONLINE 的前台互動原型，供美術設計、前端、後端與產品對齊介面功能及使用流程。
- 使用 React 18、TypeScript、Vite、Tailwind CSS 與 Lucide React，以 React Context 管理共用狀態。
- `PrototypeStage` 使用 1280 × 720 畫布並等比例縮放。不要將它描述成已完成各裝置的響應式版型。
- 目前主要使用本機 Mock 資料與狀態。畫面成功提示不代表真實登入、支付、訊息傳送、遊戲結算或後端持久化已完成。
- GitHub Pages 為既有部署方式；修改、驗證與部署應分別記錄。

## 開始工作前

1. 確認目前工作目錄、Git 根目錄、分支與 `git status --short`，保留既有修改及未追蹤檔案。
2. 閱讀本檔、本次功能相關規格與已有的決策／變更紀錄，再追查實際程式入口。
3. 先辨識本次任務是唯讀分析、文件修改、問題修正、新增功能或視覺調整，依使用者已授權的範圍執行。
4. 以「入口 → 操作事件 → 狀態／資料變化 → 畫面結果」確認行為，不只依檔名、註解、截圖或舊報告推論。

使用者在本次工作中明確提供的需求與已確認決策應納入執行；不要因本檔的一般流程重複要求已取得的確認。未決事項若不影響其他工作，先完成不受影響的部分。

## 程式與文件入口

| 位置 | 用途 |
|---|---|
| `src/App.tsx` | Provider 組裝、啟動載入、登入／大廳／遊戲室切換 |
| `src/components/layout/` | 大廳、頂部資訊、底部導航、遊戲列表、篩選與設定 |
| `src/components/features/` | 銀行、保險箱、聊天、活動、信箱、獎勵卡等功能介面 |
| `src/components/profile/`、`src/components/activity/` | 個人資訊與活動子面板 |
| `src/components/modals/` | 登入、支付、玩家資訊等彈窗 |
| `src/components/common/` | 共用畫布、彈窗外框、按鈕、提示與餘額呈現 |
| `src/components/ModalContainer.tsx` | 已註冊的全域彈窗渲染 |
| `src/context/` | 身份、錢包、導航、UI、偏好、音訊、活動與社交狀態 |
| `src/hooks/`、`src/utils/`、`src/types/` | 共用邏輯、計算與型別 |
| `src/data/mockData.tsx` | 主要 Mock 資料；其他資料也可能位於功能元件或型別檔 |
| `src/index.css`、`src/assets/` | 共用樣式與素材 |
| `docs/PLAYER_PROFILE_SPEC_v1.md` | 個人資訊規格的 Markdown 來源，含既有確認事項與 Mock 邊界 |
| `docs/VIP_PRD_Word_v3.docx` | VIP 相關需求；使用時核對版本、適用範圍與後續決策 |
| `docs/art-design-checklist.md` | 既有美術清單；入口、名稱與狀態需對照目前程式 |

`README.md`、歷史分析報告與舊截圖提供背景，但可能落後於實作。程式證明「目前怎麼運作」，已確認需求定義「應該怎麼運作」；發現差異時明確記錄，不自行把任一方視為所有情況下的最終答案。

## 架構與功能邊界

- 目前有八個 Context：`Auth`、`UI`、`Navigation`、`UserPreferences`、`Audio`、`Social`、`Activity`、`RewardCard`。修改前查看各自責任與相依關係。
- `AuthContext` 保存部分身份與個人資料至 localStorage；錢包、VIP 指標、領獎狀態與金融紀錄不等同於身份保存。其他狀態也有不同生命週期，不概括宣稱全部保存或全部不保存。
- 大廳功能主要透過導航狀態疊加顯示。部分彈窗由 `UIContext` 與 `ModalContainer` 管理，另一些由元件本身的狀態管理。
- 新增彈窗前核對觸發端與實際渲染端；不要使用未支援的 modal type，也不要假設所有彈窗都有 ESC、背景點擊或返回關閉功能。
- 既有大廳功能外框優先沿用 `LobbyModalShell` 與共用 primitives，功能狀態仍由所屬模組負責。
- 俱樂部目前在大廳被隱藏；年齡提示與新手導覽等元件是否可達，須核對實際入口。檔案存在或已註冊不等於功能已開放。
- 金幣、銀幣、銅幣、活動金幣與活動銀幣應依各自用途處理。錢包、保險箱與獎勵卡額度不可混用。
- 修改獎勵、儲值、贈禮或兌換時，一起檢查餘額、資格、重複操作、紀錄與跨頁呈現；不把 Mock 成功直接寫成正式帳務保證。

## 功能修正與新增流程

1. 記錄問題或需求、目前行為、預期行為與影響介面。已有明確規格的修正直接依規格執行，不重開已結案設計討論。
2. 新增功能或改變流程時，先釐清入口、操作前提、欄位、狀態、操作結果與跨模組影響。
3. 在授權範圍內完成修改，優先沿用現有共用邏輯；避免無關重構、整批格式化或重新設計。
4. 驗證修改本身及相關流程，依影響範圍同步規格、截圖或美術清單。尚未產出的附件明確列為未完成。
5. 分別回報實作、文件同步與驗證狀態；只有本次要求都完成，才將該項變更標為完成。

若已有變更清單，沿用其中的編號、負責範圍、決策與進度；不要為每次小修改建立重複的管理文件。需要多人協作時，依使用者要求安排分工，明確指定跨介面流程與共用檔案的整合負責人，避免多人同時覆寫相同區段。

## 通用規格書寫法

分冊重整依 `docs/spec-guidelines/APP_SPEC_RULES.md` 與新版功能模板執行；原始格式規範保存於同目錄 `PRD.md`。總規格為遷移入口，章節是否完成以遷移表為準。

- 使用繁體中文及產品功能語言，讓美術、前端、後端可共讀同一份正文。
- 每個功能按需要涵蓋：目的與範圍、入口與返回、畫面區塊、欄位與限制、操作流程、狀態與例外、跨介面影響、資料需求、驗收條件與待確認事項。
- 資料需求描述需要取得、送出與保存的內容及結果；API 路徑、schema、資料庫與樣式數值僅在任務要求時另行提供。
- 區分「已確認需求」「原型現況／限制」「待確認事項」。Mock 數值與示範帳號不得自動成為正式產品規則。
- 共用規則集中說明並由各章引用。跨頁流程寫清楚傳入資料、狀態更新、返回位置及適用的保存範圍。
- 沿用已確認的功能細節與例外，不為套用模板而新增未要求的按鈕狀態、權限或步驟。
- 已有 Markdown 來源的規格，優先修改來源；交付 Word／PDF 時同步產出並檢查呈現。尚未更新的輸出不可宣稱已同步。
- 個人資訊規格不取代 VIP 後台與排程規格；跨文件衝突記錄版本與影響，必要時只詢問尚未決定的部分。

## 驗證與指令

指令以 `package.json` 為準，使用既有 npm 與鎖定檔。

```bash
npm run dev        # 本機開發服務
npm run typecheck  # TypeScript 檢查
npm run lint       # ESLint 檢查
npm run build      # 型別檢查與 Vite 建置
npm run check      # 型別檢查、ESLint、Vite 建置
npm run preview    # 預覽建置結果
npm run deploy     # 經 predeploy 檢查後部署至 GitHub Pages
```

- 程式修改交付前執行 `git diff --check` 與 `npm run check`；失敗時區分本次引入與既有問題。
- 互動變更需確認實際入口與操作結果，依功能檢查成功、錯誤、空資料、停用或重複操作等適用情境。
- 視覺變更需在原型畫布與相關視口中檢查遮擋、溢位、文字可讀性及彈窗層級。
- 純文件修改檢查內容一致性、路徑／引用、差異與格式，不必啟動服務或執行會產出建置檔的檢查。
- 靜態檢查、建置成功、瀏覽器操作與正式服務驗證是不同證據，回報時不要互相替代。未執行的檢查說明原因。

## 範圍保護與交付

- 唯讀任務不修改、產生檔案、啟動服務、安裝套件、提交或部署，除非使用者另行要求。
- 不 reset、clean、restore 或覆寫使用者的既有工作；Git 操作只處理授權範圍，不整批 stage 無關檔案。
- Commit、push 與部署依使用者授權執行；已授權的範圍不重複詢問。部署屬發布動作，不是一般本機驗證。
- 最終回報包含變更目的、實際修改、驗證結果、文件同步情況與尚未完成項目。若有提交或推送，提供分支、commit 與結果。
